import Foundation

extension App {
    enum Core {}
    enum Coffee {}
    enum Ingredient {}
    enum DesignSystem {}
    enum Home {}
}

extension App.DesignSystem {
    enum Components {}
}

extension App.Coffee {
    enum Entities {}
    enum Persistence {}
    enum Repository {}
    enum Protocols {}
    enum Views {}
    enum Components {}
    enum AI {}
}

extension App.Ingredient {
    enum Entities {}
    enum Views {}
    enum Components {}
    enum AI {}
}

extension App.Home {
    enum Views {}
    enum Components {}
}
