import Foundation

extension App {
    enum Core {}
    enum Coffee {}
    enum Ingredient {}
    enum DesignSystem {}
}

extension App.Coffee {
    enum Entities {}
    enum Persistence {}
    enum Repositories {}
    enum Protocols {}
    enum Views {}
    enum Components {}
}

extension App.Ingredient {
    enum Entities {}
    enum Views {}
    enum Components {}
}
